{
  "name": "slime-dungeon-h5",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4174 --strictPort",
    "build": "vite build",
    "test": "node --test",
    "preview": "vite preview --host 0.0.0.0 --port 4174 --strictPort"
  },
  "dependencies": {
    "phaser": "4.0.0"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
